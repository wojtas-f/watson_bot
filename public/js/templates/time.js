export const timeTemplate = time => {
    return `
    <div class="eight wide column time-display">
        <div class="time-container">
            <p class="time">${time}</p>
        </div>    
    </div>
    `
}
